_BASE_: ../base-prompt.yaml

DATA:
  NAME: "OxfordFlowers"
  DATAPATH: "/content/data/oxford-flowers102"   # <-- change if your path is different
  NUMBER_CLASSES: 102
  MULTILABEL: False

MODEL:
  TYPE: "vit"
  TRANSFER_TYPE: "prompt"   # use VPT / visual prompts

SOLVER:
  TOTAL_EPOCH: 10
  BASE_LR: 5.0e-3
  WEIGHT_DECAY: 0.0001

RUN_N_TIMES: 1
